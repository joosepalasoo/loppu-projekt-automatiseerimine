# Connection details for the Ansible deployment target
ANSIBLE_HOST=192.168.5.240
ANSIBLE_USER=kasutaja

# Paste the private SSH key used to connect to the target host.
# Keep this value in repository or organization secrets and do not commit real keys.
ANSIBLE_SSH_PRIVATE_KEY=-----BEGIN OPENSSH PRIVATE KEY-----
